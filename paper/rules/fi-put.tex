\framebox{\( \turnsput (\ty; l; e : \ty \yieldsnothing E) : (\ty, \ty) \yieldsnothing C \)}

\inferrule [PutBase]
{}
{\turnsput (\RecTy l \ty; l; e : \ty_1) : (\ty, \RecTy l {\ty_1})}

\inferrule [PutLeft]
{\turnsput (\ty_1; l; e : \ty) : (\ty_3, \ty_4)}
{\turnsput (\ty_1 \Intersect \ty_2; l; e : \ty) : (\ty_3, \ty_4 \Intersect \ty_2)}