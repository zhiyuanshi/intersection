\framebox{\( \turnsget (\tau_1; l) : \tau_2 \yields C \)}

% The field with label $ l $ inside $ \tau_1 $ is of type $ \tau_2 $.

\inferrule [GetBase]
  {}
  {\turnsget (\RecTy l \tau; l) : \tau \yields {\Lam x {\image {\RecTy l \tau}} x}}

\inferrule [GetLeft]
{\turnsget (\tau_1; l) : \tau \yields C}
{\turnsget (\tau_1 \Intersect \tau_2; l) : \tau
  \yields {\Lam x {\image {\tau_1 \Intersect \tau_2}} {C (\Proj k x)}}}
