\framebox{\( \turnsput (\ty; l; e : \ty \yields E) : (\ty, \ty) \yields C \)}

\inferrule [PutBase]
{}
{\turnsput (\RecTy l \ty; l; e : \ty_1 \yields E) : (\ty, \RecTy l {\ty_1})
  \yields {\Lam x {\image {\RecTy l \ty}} E}}

\inferrule [PutLeft]
{\turnsput (\ty_1; l; e : \ty \yields E) : (\ty_3, \ty_4) \yields C}
{\turnsput (\ty_1 \Intersect \ty_2; l; e : \ty \yields E) : (\ty_3, \ty_4 \Intersect \ty_2)
  \yields {\Lam x {\image {\ty_1 \Intersect \ty_2}} {C (\Proj k x)}}}
