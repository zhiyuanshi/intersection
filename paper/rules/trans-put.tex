\framebox{\( \Gamma \vdash_{put} (t; l; e) : t \yields C \)}

\infrule[PutBase]
{\Gamma \vdash e : t' \yields E \andalso t' \subtype t}
{\Gamma \vdash_{put} (\recordtype l t; l; e) : \recordtype l {t'}
  \yields {\abs {\rel x {\imageof {\recordtype l t}}} E}}

\infrule[PutLeft]
{\Gamma \vdash_{put} (t_1; l; e) : t_1' \yields C}
{\Gamma \vdash_{put} (t_1 \with t_2; l; e) : t_1' \with t_2
  \yields {\abs {\rel x {\imageof {t_1 \with t_2}}} {C (\fst ~ x)}}}

\infrule[PutRight]
{\Gamma \vdash_{put} (t_2; l; e) : t_2' \yields C}
{\Gamma \vdash_{put} (t_1 \with t_2; l; e) : t_1 \with t_2'
  \yields {\abs {\rel x {\imageof {t_1 \with t_2}}} {C (\snd ~ x)}}}
